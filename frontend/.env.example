# Base URL for the backend API (no trailing slash)
VITE_API_BASE_URL=/api/v1
# OAuth navigation should point directly to the backend, not the Vite proxy.
VITE_OAUTH_API_BASE_URL=http://127.0.0.1:8000/api/v1
